# InsightFactory Agent Rules

This directory contains configuration, rules, and skills for the AI agent working on this repository.
- Agent should follow the backend/frontend separation rules.
- Backend logic should go into `backend/`.
- Frontend logic should go into `frontend/`.
- All documentation should go into `docs/`.

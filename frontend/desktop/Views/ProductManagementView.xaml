<UserControl x:Class="InsightFactory.Desktop.Views.ProductManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InsightFactory.Desktop.Views"
             xmlns:vm="clr-namespace:InsightFactory.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="#F4F6F9">
             
    <UserControl.DataContext>
        <vm:ProductManagementViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Input Form -->
        <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="#DDDDDD" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Product Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333"/>
                
                <TextBlock Text="Code" FontSize="14" Foreground="#666" Margin="0,0,0,5"/>
                <TextBox Text="{Binding InputCode, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" Margin="0,0,0,15"/>
                
                <TextBlock Text="Revision" FontSize="14" Foreground="#666" Margin="0,0,0,5"/>
                <TextBox Text="{Binding InputRevision, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" Margin="0,0,0,25"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="등록" Command="{Binding AddProductCommand}" Background="#4CAF50" Foreground="White" FontWeight="Bold" Padding="10" BorderThickness="0" Cursor="Hand"/>
                    <Button Grid.Column="2" Content="수정" Command="{Binding UpdateProductCommand}" Background="#2196F3" Foreground="White" FontWeight="Bold" Padding="10" BorderThickness="0" Cursor="Hand"/>
                </Grid>
                <Button Content="입력 초기화" Command="{Binding ClearFormCommand}" Background="#E0E0E0" Foreground="#333" FontWeight="Bold" Padding="10" BorderThickness="0" Margin="0,10,0,0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Right Panel: Data Grid -->
        <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="#DDDDDD" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Product List" FontSize="18" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>
                    <Button Content="선택 삭제" Command="{Binding DeleteProductCommand}" HorizontalAlignment="Right" Background="#F44336" Foreground="White" FontWeight="Bold" Padding="15,8" BorderThickness="0" Cursor="Hand"/>
                </Grid>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" RowBackground="White" AlternatingRowBackground="#F9F9F9" GridLinesVisibility="None" BorderThickness="1" BorderBrush="#E0E0E0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="*"/>
                        <DataGridTextColumn Header="Revision" Binding="{Binding Revision}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
